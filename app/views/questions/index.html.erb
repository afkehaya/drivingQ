
<p id="notice"><%= notice %></p>
<% if user_signed_in? %>

 <%= @user.name %>
  <div class="row">
   <div class="col-md-8">
      <h1>What question is driving you right now?</h1>
      <div class="question-top">
        <%= render 'new' %>
      </div>
      <div class="demo-walk">
        <h2>How it works</h2>
        <h4>Now that you've created your account, you have access to the referral widget. Click the button bellow and share the link with a friend.</h4>
        <h4>When your friend visits the <strong>success page</strong> on the nav bar our software will count that as a successful referral</h4>
        <h4>For each person you refer, you'll see your referral count go up by one and earn rewards.</h4>
          <%= render 'layouts/refer' %>
      </div>
      <h3>About DrivingQ</h3>
      <h4>This is a fictional software product that we created to show you how our referral widget works.</h4>
      <h5>Each day you should ask yourself one critical question that drives you towards reaching your goal.</h5>
      <h5>For Oracle's champion Sailboat raicing team it's "Does this make the boat go faster".</h5>
      <h5>Create your driving question and then provide a list of tasks. If you ask your self your driving question about each task... the answer should be yes.</h5>

      <h5>Use this app to stay focused and motivated. Track your questions and how many of your tasks you achieve each day.</h5>
   </div>

   <div class="col-md-4">
     <div class="questions-list">
        <h2>My Driving Questions</h2>
          <% @questions.each do |question| %>
          <% if question.user == current_user %>
            <ul>
              <li>
                <h3><%= link_to question.question, question %>
                <%= link_to '', edit_question_path(question), class: "glyphicon glyphicon-edit" %>
                <%= link_to '', question, class: "glyphicon glyphicon-trash", method: :delete, data: { confirm: 'Are you sure?'} %>
                </h3>
             </li>
            </ul>
            <% end %>
          <% end %>
      </div>
    </div>
  </div>

<% else %>
<script type="text/javascript" src="https://api.actionwins.co/click.js"></script>
<%= render "landing" %>

<% end %>
